function convertirStringAMayusculas(array) {
  // Convierte a mayúsculas todos los strings del array.
  // Retorna el arreglo resultante.
  // Tu código:
 var may= array.map((x)=>x.toUpperCase())
 return may
}

module.exports = convertirStringAMayusculas;
